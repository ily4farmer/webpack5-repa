import { Container, Divider } from '~ui';

import { MainStack } from './MainStack';

export const Skills = () => (
  <section>
    <Container>
      <Divider>Навыки</Divider>

      <MainStack />
    </Container>
  </section>
);
